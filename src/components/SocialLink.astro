---
import { buttonVariants } from "./ui/button";

type Props = {
  href: string;
  label: string;
};

const { href, label } = Astro.props;
---

<a class={buttonVariants({ variant: "ghost", size: "icon" })} href={href}>
  <slot />
  <span class="sr-only">{label}</span>
</a>
